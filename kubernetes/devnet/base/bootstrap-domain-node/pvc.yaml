apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bootstrap-archival-node-pvc
  namespace: bootstrap-domain
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-storageclass
  resources:
    requests:
      storage: 100Gi
