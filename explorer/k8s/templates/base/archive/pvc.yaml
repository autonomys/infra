# pvc.yaml
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: archive-data-pvc
  namespace: explorer-network
  labels:
    app: archive-squid-app
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: efs-sc
  resources:
    requests:
      storage: 150Gi
