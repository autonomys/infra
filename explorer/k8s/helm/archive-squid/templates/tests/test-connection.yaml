apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "archive-squid.fullname" . }}-test-connection"
  labels:
    {{- include "archive-squid.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "archive-squid.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
