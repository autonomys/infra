apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "explorer-squid.fullname" . }}-test-connection"
  labels:
    {{- include "explorer-squid.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "explorer-squid.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
