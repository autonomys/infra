# DEPRECATED

This project has been replaced by:

- **Module**: `modules/auto-drive/` — reusable infrastructure module
- **Production**: `resources/terraform/auto-drive-production/` — production environment config
- **Staging**: `resources/terraform/auto-drive-staging/` — planned, not yet configured

Do not use this directory for new work. It is retained for reference only and
will be removed in a future cleanup.
